# prediction/urls.py
from django.urls import path
from .views import PricePredictionView, UserPredictionsView

urlpatterns = [
    path('predict-price/', PricePredictionView.as_view(), name='predict-price'),
    path('user-predictions/', UserPredictionsView.as_view(), name='user-predictions'),
]